# Coverage configuration file
# Every module under src/ is measured. To exclude a module, add its pattern
# to the omit list below.
# This file is the single source of truth for coverage measurement (CI, pytest.ini
# and check_coverage.py all use it).

[run]
# Source directories to measure coverage for
source = src

# Omit patterns - files matching these patterns will NOT be included in coverage
omit =
    # Always exclude these
    */tests/*
    */test_*.py
    */__pycache__/*
    */htmlcov/*
    */.pytest_cache/*
    */venv/*
    */env/*
    */.venv/*

[report]
# Exclude lines from coverage reports
exclude_lines =
    # Have to re-enable the standard pragma
    pragma: no cover

    # Don't complain about missing debug-only code
    def __repr__
    if self.debug

    # Don't complain if tests don't hit defensive assertion code
    raise AssertionError
    raise NotImplementedError

    # Don't complain if non-runnable code isn't run
    if __name__ == .__main__.:

    # Don't complain about abstract methods
    @abstractmethod

# Precision for coverage percentage
precision = 2

# Show missing line numbers
show_missing = True

# Skip empty files
skip_empty = True

